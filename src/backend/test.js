const {db} = require('./database');

// exports.getName = () => {
//     const sql = `SELECT * FROM settings;`

//     const stmt = db.prepare(sql);
//     const res = stmt.all();
//     return res;
// }